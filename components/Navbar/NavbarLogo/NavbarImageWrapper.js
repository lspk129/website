import styled from 'styled-components';

const NavbarImageWrapper = styled.a`
  display: grid;
  justify-content: center;
  align-content: center;
  color: #fff;
  font-size: 20px;
`;

export default NavbarImageWrapper;
