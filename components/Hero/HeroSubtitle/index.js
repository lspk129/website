import styled from 'styled-components';

const HeroSubtitle = styled.h2`
  color: #fff;
  text-align: center;
  grid-column: 2 / 3;
  grid-row: 3 / 4;
  align-self: center;
  justify-self: center;
`;

export default HeroSubtitle;
